@page
@model DigitalBanking.Dashboard.Pages.IndexModel
@{
    ViewData["Title"] = "Dashboard";
}

<div class="row">
    <div class="col-md-8">
        <div class="card shadow-sm">
            <div class="card-body">
                <h4>Admin Dashboard</h4>
                <p class="text-muted mb-0">Baseline dashboard hazır. Menüden raporlara geç.</p>
            </div>
        </div>
    </div>
</div>